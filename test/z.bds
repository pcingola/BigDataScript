#!/usr/bin/env bds

print("Start\n")

print("Loop:\n")
for( string s : ['one', 'two', 'three'] ) {
	print("\ts : $s\n")
	if( s == 'two' ) {
		checkpoint
		print("\t--- checkpoint ---\n")
	}
}
print("Done!\n")
