#!/usr/bin/env bds

#void foreach(string[] l) {
#	for( string s : l ) println s
#}
#int f(string s)		return s.length()

println "Start"

a := ['hi', 'bye', 'adios']

la := a.map( length )
println "la: $la"
