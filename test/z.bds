
in := '/tmp/in.txt'
out := '/tmp/out.txt'
doit := true
hi := 'hello'

#task( out <- in, doit, hi := 'hello') {
task(out <- in) {
	sys echo "$hi"; pwd -P ;\
		echo running; \
        sleep 2; \
		echo done | tee $out
}

#wait
#task echo bye
