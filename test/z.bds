#!/usr/bin/env bds

in := 'in.txt'
out := 'out.txt'
map := { in => out }

print "map : $map\n"

# New input file
sys date > $in

task( map.values() <- map.keys() ) {
	sys echo RUNNING TASK ; cp $in $out
}

