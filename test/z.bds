#!/usr/bin/env bds

in := "in.txt"
out := "out.txt"
out2 := "out2.txt"

sys touch $in $out $out2; rm $in $out $out2; date > $in
task( out <- in ) sys echo Hi ; sleep 1 ; cat $in > $out
task( out2 <- out ) sys echo Hi 2 ; sleep 1 ; cat $out > $out2
