#!/usr/bin/env bds

# Function
string hi(string s)		return 'hi:' + s

bool hasI(string s)		return s.indexOf('i') >= 0

int len(string s)		return s.length()

println "Start"

a := ['hi', 'bye', 'adios']

la := a.filter( hasI ).map( hi ).mapToInt( len ).sum()
println "la: $la"
