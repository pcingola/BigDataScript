#!/usr/bin/env bds

# A loop fires threads of funcions
par {
	for(int i : range(1, 10)) {
		println("\tPAR i: $i")
		sleep(0.5)
	}
}


for(int i : range(1, 10)) {
	println("MAIN i: $i")
	sleep(1.0)
}
