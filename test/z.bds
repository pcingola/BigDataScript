#!/usr/bin/env bds

int z(int x) {
	return x + 1;
}

int f(int x) {
	j := z(x)
	return j + 1;
}

breakpoint "We break here\n"

for( int i=0 ; i < 6 ; i = f(i) ) {
	print "hi $i\n"
	debug "Variable: $i\n"
}

print "Done\n"
