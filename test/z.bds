#!/usr/bin/env bds

sleepTime := 0.2

par {
	for( int i : range(1,10) ) {
		print("par $i\n")
		sleep( sleepTime )
	}
}

for( int i : range(1,10) ) {
	print("main $i\n")
	sleep( sleepTime )
	if( i == 3 ) { 
		print("Checkpoint\n")
		checkpoint "test/z.chp"
	}
}
