#!/usr/bin/env bds

#void foreach(string[] l) {
#	for( string s : l ) println s
#}

string len(string s)		return 'hi:'+s

println "Start"

a := ['hi', 'bye', 'adios']

la := a.mapToInt( len )
println "la: $la"
