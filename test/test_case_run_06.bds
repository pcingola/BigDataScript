#!/usr/bin/env bds

int f(int x) {
	println "OK"
	return 2 + x
}

class A {
	int f(int x) {
		if( x < 0 ) {
			println "NEGATIVE"
			return -1
		}
		return 1 + x
	}
}

void test01() {
	a := new A()
	z := a.f(2)
	assert(3, z)
}

void p()	println "YOU SHOULD NOT SEE THIS MESSAGE WHEN RUNNING TESTS"
