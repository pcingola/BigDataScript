
include 'aws_util'

println "Before"

out := "$s3base/out.txt"
println "Output file: '$out'"

task(system := 'aws', detached := true) {
	sys echo HI
	sys date > tmp.txt
	sys aws s3 cp tmp.txt "$out"
	sys sleep 1
	sys echo BYE
}
println "After"
wait
println "Done"
