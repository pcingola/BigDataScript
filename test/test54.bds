
str := 'a'

switch( str ) {
    case 'a':
        println 'A'
        break

    case 7:
        println 'B'
        break

    default:
		# Default in the middle of the switch statement (note that it does not fall-through)
        println 'Unknown'
}

println 'Done'
