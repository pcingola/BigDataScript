#!/usr/bin/env bds

par {
	for( int i=0 ; i < 10 ; i++ ) {
		if( i == 4 ) {
			print "Checkpoint\n"
			checkpoint 'test/zz.chp'
		}
		print "par: $i\n";
		sleep(1)
	}
}

for( int i=0 ; i < 10 ; i++ ) {
	print "main: $i\n";
	sleep(1)
}
