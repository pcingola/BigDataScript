#!/usr/bin/env bds

done := false

void zzz(int n) {
	if( n > 1 ) {
		if( ! done ) {
			done = true
			print "CHECKPOINT!!!\n"
			checkpoint "test/z.chp"
		}
		return
	}

	print "zzz($n)\n"
	for(int i=0; i < n ; i++ ) {
		pid := par zzz(n+1)
		print "\t$pid\n"
	}
}

zzz(1)
