#!/usr/bin/env bds


string out

for( int i : range(0, 10) ) {
	if( i == 1 )	continue;
	print "i: $i\n";

	for( int j : range(0, 10) ) {
		if( j == 3 )	continue;
		print "\tj: $j\n";
		out = "$i\t$j"
		if( j == 7 )	break;
	}

	if( i == 5 )	break;
}


print "out = '$out'\n"
